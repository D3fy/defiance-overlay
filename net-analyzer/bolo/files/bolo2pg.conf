# conf.d file for bolo2pg

USER="bolo"
GROUP="bolo"

# The endpoint is the bolo subscriber endpoint
# Where bolo2rrd connectes to and looks for events
ENDPOINT="tcp://127.0.0.1:2999"

# Postgres related conf
HOST="127.0.0.1"
PORT="5432"
DATABASE="bolo"
CREDS="/etc/bolo/pg.creds"
