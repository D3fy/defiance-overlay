# you can change the init script behavior by setting those parameters
# - group (default: heketi)
# - pidfile (default: /run/heketi/heketi.pid)
# - user (default: heketi)

# extra arguments for heketi
command_args=""

# upstream strongly recommends > 1
GOMAXPROCS=2
